<tr>
  <td><%= subpitch.id %></td>
  <td><%= subpitch.pitch.name %></td>
  <td><%= subpitch.subpitch_type.name %></td>
  <td><%= subpitch.name %></td>
  <td><%= subpitch.description %></td>
  <td><%= subpitch.price_per_hour %></td>
  <td><%= subpitch.currency %></td>
  <td><%= image_tag subpitch.picture, style: "max-width: 200px; max-height: 200px;" if subpitch.picture.attached? %></td>
  <td><%= subpitch.size %></td>
  <td>
    <%= link_to t(".edit"), edit_admin_pitch_subpitch_path(subpitch.pitch.id, subpitch) %>
    <%= link_to t(".delete"), admin_pitch_subpitch_path(subpitch.pitch.id, subpitch), method: :delete, data: {confirm: t(".confirm")} %></td>
</tr>
