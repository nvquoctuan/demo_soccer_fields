<% provide :title, t(".title") %>
<h1><%= t ".title" %></h1>
<div>
  <%= form_with url: admin_pitch_subpitch_ratings_path(@subpitch.pitch.id, @subpitch), local: true, class: "input-group", method: "get" do |f| %>
    <span class="input-group-prepend left">
      <%= f.button "", class: "btn btn-primary" do %>
        <svg class="c-icon" class="h14 w20">
          <use xlink:href="/assets/admin/coreui/free-symbol-defs.svg#cui-magnifying-glass"></use>
        </svg> <%= t "general.search" %>
      <% end %>
    </span>
    <%= f.text_field :search, class: "form-control wid30 left", id: "input1-group2",value: params[:search] %>
  <% end %>
</div>
<hr class="mt-0">
<table class = "table table-responsive-sm table-hover table-outline mb-0">
  <thead class="thead-light">
    <tr>
      <th class = "text-center"><%= t ".id" %></th>
      <th class = "text-center"><%= t ".user" %></th>
      <th class = "text-center"><%= t ".star" %></th>
      <th class = "text-center"><%= t ".content" %></th>
      <th class = "text-center"><%= t ".option" %></th>
    </tr>
  </thead>
  <tbody>
    <%= render partial: "rating", collection: @ratings, locals: {subpitch: @subpitch} %>
  </tbody>
</table>
<hr class="mt-0">
<%= will_paginate @ratings %>
